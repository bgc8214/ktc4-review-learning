# 개념 실험실: 기초 지식과 정확성 검토

작성·검토: 2026-09-13. 기존 리뷰를 학습 질문으로 삼고 공식 문서로 핵심 규칙을 교차 확인했다. 이 문서는 모델 자체 검토 기록이며 외부 전문가의 보증은 아니다. 애니메이션의 시간·동전·장치·보관함은 설명용 비유다.

## 학습 설계

주제마다 ‘무엇이 이동하는가 → 어느 상태가 바뀌는가 → 잘못 설계하면 무엇이 달라지는가’를 먼저 정했다. 정답 버튼 대신 직접 조작 순서를 정하고 정책을 바꿔 비교한다. 각 실험은 독립적이며 실제 요청의 필수 순서가 아니다. 시각 모형 상세는 VISUAL_MODEL.md에 기록했다.

| 도시 | 선수 개념 | 움직이는 대상 | 비교 실험 | 확인할 결과 |
| --- | --- | --- | --- | --- |
| 로그인과 인가 | HTTP 요청, 사용자 ID | 로그인 요청 → 세션 ID → 카드 요청 | 소유권 검사 켜기/끄기 | B의 카드가 A에게 반환되는가 |
| 토큰 재발급 | 공유 상태, 요청 교차 실행 | 요청 A와 B | GET/DEL 분리 vs GETDEL | 같은 기존 토큰을 몇 요청이 소비하는가 |
| 트랜잭션 은행 | DB 행, 커밋 | 이체 명령 | 입금 실패: 트랜잭션 있음/없음 | 잔액 합계가 보존되는가 |
| 비동기 작업 | 브라우저 메모리, 서버 저장소 | 생성 요청 → jobId → 상태 조회 | 새로고침 후 ID 복구 있음/없음 | 서버 완료와 화면 복구를 구분하는가 |
| 포트와 어댑터 | 인터페이스, 의존성 | 커밋 조회 요청 | GitHub 구현체 vs 테스트 대역 | 같은 내부 약속으로 다른 구현체를 쓰는가 |
| AI 근거 공장 | 생성과 사실 확인 | 후보 → 초안 → SHA → 판정 | 정상 SHA vs 없는 SHA | 조회 실패 시 확정을 차단하는가 |

## 1. 로그인과 인가

서버는 세션 ID와 사용자·만료 정보를 연결한다. 쿠키는 세션 ID를 전달하는 수단이며, JWT는 토큰 형식이다. 로그인 확인 후에도 자원 소유권 검사가 필요하다. 예시는 A가 B의 카드 UUID를 알고 있다는 조건으로 고정한다.

검토: UUID의 추측 난이도와 접근 권한은 다르다. 세션 ID가 유출되면 정보가 안 담겨 있어도 세션 탈취 위험이 있다. HttpOnly는 JS의 쿠키 읽기를 제한하며 XSS 자체를 없애지는 않는다. 쿠키 만료와 서버 세션 만료는 별개다. 게임은 실제 로그인이나 토큰을 사용하지 않는다.

- [Spring Security 인가](https://docs.spring.io/spring-security/reference/servlet/authorization/index.html)
- [MDN 쿠키](https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Cookies)
- [JWT RFC 7519](https://www.rfc-editor.org/rfc/rfc7519)
- 기존 리뷰 연결: 경북 1팀 #4, 부산 3팀 #29.

## 2. 토큰 재발급과 원자성

GET 후 DEL 사이에 다른 요청이 끼어들면 A와 B가 모두 토큰을 읽을 수 있다. GETDEL은 값 반환과 삭제를 한 명령으로 처리한다. 예시는 두 요청이 같은 기존 토큰을 제출했고 검증 조건이 충족됐다고 가정한다.

검토: 사용자가 두 요청의 읽기와 삭제 순서를 정한다. A 읽기→A 삭제→B 읽기이면 분리 명령이어도 중복되지 않는다. A 읽기→B 읽기→삭제이면 중복될 수 있다. GETDEL만으로 안전한 토큰 재발급 전체가 완성되지는 않는다. 입력 검증·소비 후 장애·응답 유실·새 토큰 저장은 별도 설계다. Redis 6.2부터 제공되는 문자열 명령이다.

- [Redis GETDEL 공식 문서](https://redis.io/docs/latest/commands/getdel/)
- [한국어 심화: 우아한형제들 재고 관리](https://techblog.woowahan.com/2709/)
- 기존 리뷰 연결: 강원 3팀 #59의 재발급 동시성 피드백.

## 3. 트랜잭션

A=100, B=100에서 30을 이체한다. 출금 뒤 입금 오류가 발생할 때 하나의 DB 트랜잭션이면 두 변경을 취소할 수 있다. 별도 자동 커밋이면 출금만 남을 수 있다. 숫자는 교육용 예시다.

검토: 미커밋 변경은 다른 세션에 보이는 확정 잔액이 아니다. 그림의 임시 잔액을 명시한다. 원자성과 격리성은 다르며, 이 그림은 격리 수준·잠금·실제 계좌 시스템을 재현하지 않는다. DB 롤백이 외부 HTTP 요청까지 되돌리지는 않는다.

- [PostgreSQL 트랜잭션](https://www.postgresql.org/docs/current/tutorial-transactions.html)
- [한국어 심화: 우아한형제들 재고 관리](https://techblog.woowahan.com/2709/)
- 기존 리뷰 연결: 강원 3팀 #59의 Service/Usecase 트랜잭션 질문.

## 4. 비동기 Job

생성 응답의 jobId로 작업을 조회한다. 202 Accepted는 접수이며 성공 완료가 아니다. 브라우저 메모리가 초기화되어도 서버 작업은 독립적으로 진행할 수 있다. 예시는 서버가 작업을 지속하는 구현이라고 가정한다.

검토: ID 보관 자체는 실행 재개가 아니다. 서버 상태 재조회와 폴링 복구가 필요하다. 완료·실패 시 폴링 중단, 소유권 확인, 타이머 정리, 오류 재시도 규칙은 실제 구현에서 추가해야 한다. 서버 재시작 복구는 이 그림의 범위 밖이다.

- [MDN 202 Accepted](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status/202)
- [한국어 참고: 카카오 Mocking](https://tech.kakao.com/posts/458)
- 기존 리뷰 연결: 경북 1팀 #4 Job 폴링.

## 5. 포트와 어댑터

업무는 CommitReader라는 약속을 사용한다. GitHubAdapter와 FakeAdapter는 같은 약속을 수행한다. 이동 경로는 실행 흐름이며 소스 코드 의존 방향과 같다고 해석하면 안 된다. 포트와 업무가 안쪽에, 기술 어댑터가 바깥쪽에 있다.

검토: 인터페이스 하나가 전면 헥사고날을 뜻하지 않는다. 외부 구현 교체 외에도 기술 격리·테스트가 이유가 될 수 있다. ‘ingest만 GitHub에 접근’은 모듈의 책임 제한이며 포트의 충분한 근거는 아니다. 모듈러 모놀리스와 레이어드는 함께 사용할 수 있다.

- [Alistair Cockburn: Hexagonal Architecture](https://alistair.cockburn.us/hexagonal-architecture)
- [한국어 사례: 카카오뱅크 메시지 허브](https://tech.kakaobank.com/posts/2311-hexagonal-architecture-in-messaging-hub/)
- 기존 리뷰 연결: 경북 1팀 #4 선택적 포트.

## 6. AI 근거 검증

규칙 기반 후보 압축 이후 모델이 초안과 SHA를 제안한다. 검증기는 저장소 커밋을 재조회한다. 없으면 확정하지 않는다. 정상 예시는 존재·대상 저장소·작성자·관련성 확인까지 충족했다고 가정한다.

검토: SHA 존재는 서술 전체의 사실성을 보증하지 않는다. 조회 실패는 없는 SHA 외에도 인증·권한·네트워크 오류 때문일 수 있다. 게임은 ‘접근 가능한 저장소에서 없는 SHA’와 ‘SHA는 있지만 주장이 실제 변경과 다른 경우’를 구분한다. 일반적인 자연어 검증 알고리즘이 아니라 미리 만든 사례다. FastAPI가 LLM인 것은 아니며 GitHub API 요청도 LLM 호출이 아니다.

- [GitHub 커밋 조회](https://docs.github.com/en/rest/commits/commits#get-a-commit)
- [한국어 테스트 참고: 토스](https://toss.tech/article/test-strategy-server)
- 기존 리뷰 연결: 경북 1팀 #4 evidence 경계.

## 구현 전 검토 결과

바로잡은 표현: ‘JWT 조기 취소 불가’는 추가 상태 검사 시 가능, ‘모든 포트는 교체 목적’은 기술 격리 목적도 가능, ‘SHA가 있으면 사실’은 관련성 검증도 필요, ‘새로고침하면 작업 소멸’은 브라우저와 서버 상태 구분이 필요하다.

한국어 블로그는 기존 학습 사이트에서 확인한 참고 글을 유지한다. 공식 문서와 설명의 범위를 연결했지만, 블로그나 학생 발언을 무조건 정답으로 취급하지 않는다. 시뮬레이션 핵심 결과는 자동 테스트로 확인한다.
